// Problem:33
function sumCSV(s) {
    // Split the string by commas, convert each part to a number, and sum them
    return s.split(',').reduce((sum, num) => sum + parseFloat(num.trim()), 0);
}

console.log(sumCSV("1.5, 2.3, 3.1, 4, 5.5, 6, 7, 8, 9, 10.9")); // Output: 57.3
